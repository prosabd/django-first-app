services:
  web:
    build: .
    command: bash docker-entrypoint.sh
    volumes:
      - /app
    ports:
      - "8001:8001"
    environment:
      - DEBUG=${debug}
      - db_host=${db_host}
      - db_port=${db_port}
      - db_user=${db_user}
      - db_password=${db_password}
      - db_name=${db_name}
>>>>>>> 11740b7 (feat: :rocket: docker config for master prod env)
    env_file:
      - .env

networks:
  django:
    driver: bridge
